<div style="float: right; margin: 0 1em 1em 0;"><img src="../Images/orderedList3.png"></div>

@model IEnumerable<University.Models.ControlType>

@{
    ViewBag.Title = "Види контролю";
}

<p>
<h2>Види контролю</h2>

<p>
    @Html.ActionLink("Додати новий", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Редагувати", "Edit", new { id=item.ControlTypeId }) |
            @Html.ActionLink("Переглянути", "Details", new { id=item.ControlTypeId }) |
            @Html.ActionLink("Видалити", "Delete", new { id=item.ControlTypeId })
        </td>
    </tr>
}

</table>



</br></br>